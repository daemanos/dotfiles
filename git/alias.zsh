# use hub, because it's awesome
# http://defunkt.github.com/hub/
alias git=$(which hub)

# miscellanea
alias gd='git diff'
alias gc='git commit'
alias gca='git commit -a'
alias gco='git checkout'
alias gb='git branch'
alias gs='git status -sb'
