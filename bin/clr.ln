#!/usr/bin/bash
for fn in "$@" ; do
    if [ -d $fn ]; then
        rm -r $fn
        mkdir $fn
    elif [ -f $fn ]; then
        rm $fn
        touch $fn
    else
        echo -e "\e[1;31merror:\e[0m no such file or directory: $fn"
    fi
done
