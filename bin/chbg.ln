#!/bin/bash
if [ -f $1 ]; then
    pic=$1
elif [ -f $HOME/img/walls/$1 ]; then
    pic=$HOME/img/walls/$1
else
    echo -e "\e[1;31merror:\e[0m no such file: $1"
    exit 1
fi

ln -sf $pic $HOME/img/desktop
setbg
